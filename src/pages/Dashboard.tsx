import { FC } from 'react';
import Panel from 'templates/Panel';

type DashboardProps = {};

const Dashboard: FC<DashboardProps> = () => {
  return <Panel>در حال تکمیل ...</Panel>;
};

export default Dashboard;
