import { createTheme } from '@mui/system';

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#ed5513',
    },
    secondary: {
      main: '#50c7dF',
      dark: '#317db0',
    },
  },
  direction: 'rtl',
});
